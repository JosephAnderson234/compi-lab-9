struct Rect {
 var int w;
 var int h;
};

fun int main()
 var Rect r;
 var int area;
 r = new Rect{3, 4};
 if (r.w > r.h) then
   area = r.w * r.w
 else
   area = r.w * r.h
 endif;
 print(area);
 return(0)
endfun
